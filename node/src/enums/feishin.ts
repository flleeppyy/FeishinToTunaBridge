export enum PlayerStatus {
  PAUSED = 'paused',
  PLAYING = 'playing',
}

export enum FeishinLibraryItem {
  ALBUM = 'album',
  ALBUM_ARTIST = 'albumArtist',
  ARTIST = 'artist',
  GENRE = 'genre',
  PLAYLIST = 'playlist',
  SONG = 'song',
}

export enum FeishinRepeatState {
  ALL = "all",
  NONE = "none",
  ONE = "one",

}